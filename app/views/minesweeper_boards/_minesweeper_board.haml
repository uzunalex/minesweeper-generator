-# - debugger
%table.table.table-bordered
    - @minesweeper_board.board_state.each do |row|
        %tr
        - row.each do |cell|
            %td{ style: "height: 50px; width: 50px; text-align: center;" }
                = cell['bomb'] == 1 ? '💣' : "\n" 